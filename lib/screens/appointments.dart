import 'package:flutter/material.dart';

class Apponintments extends StatelessWidget {
  const Apponintments({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Appointments Page'));
  }
}
